// toggle showing the `save the date` title
export let saveTheDate = true;

// toggle showing the `our story` section
export let showStory = false;

// toggle showing the events section
export let showEvents = false;

// toggle between being able to RSVP and signing up for the email update list
export let canRsvp = false;

// toggle showing the lodging section
export let showLodging = false;

// toggle showing the registry section
export let showRegistry = false;

// toggle showing the `things to do` section
export let showActivities = false;
